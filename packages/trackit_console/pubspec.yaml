name: trackit_console
description: Trackit logger extension and outputting logs to the console. Use this with Dart applications.
version: 0.1.0+1
repository: https://github.com/unger1984/trackit

funding:
  - https://boosty.to/unger1984

topics:
  - cross-platform
  - logging
  - logger
  - logs
  - log

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=3.0.0 <3.6.0"

dependencies:
  meta: ^1.16.0
  trackit: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
